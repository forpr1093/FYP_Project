import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fyp.routed',
  appName: 'Routed',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
